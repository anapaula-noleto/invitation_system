// LEGACY TYPES - Photo Generation (Removed from main app)

export type GenerationMode = 'retouch' | 'generate';

export interface CoupleDetails {
  partner1Description: string;
  partner2Description: string;
  outfitStyle: string;
  setting: string;
}
